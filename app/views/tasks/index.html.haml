- content_for(:title, 'Your Tasks')
.container
	.row
		.col-sm-6
			.panel.panel-info
				.panel-heading
					%ul.nav.nav-pills{role: "tablist"}
						%li.active.panel-title
							%a{"data-toggle" => "pill", href: "#home", role: "tab"} Add a new task
						%li.panel-title
							%a{"data-toggle" => "pill", href: "#profile", role: "tab"} Find optimized tasks
				.tab-content
					#home.tab-pane.active 
						.panel-body
							= render 'form'
					#profile.tab-pane 
						.panel-body
							Free time task generation (enter your mindset/tags you would be okay with (either all or specify manually) and enter the amount of time you have available and the task or two you want to do for sure and it will use all this info + your location to figure out what tasks you can do). Free time attribute in users table for now.
							When you submit form:
							(hidden field)
							saves to current_user.current_location = ...
							(normal field to input)
							saves to current_user.free_time
							@tasks = Task.near(current_user.current_location, 10)
							@tasks.each do |task|
								if task.est_time <= current_user.free_time
									 link_to task.name, task_path(t)
									 (BE ABLE TO CLICK BOX LIKE IN FLAT UI AND SELECT TASKS THAT YOU WANT AND IT AUTOMATICALLY IMPORTS INTO ICLOUD CALENDAR
									 	(AlSO, BE ABLE TO CREATE CALENDAR IN ICLOUD CALENDAR THAT WHEN YOU ADD TASK OR SWITCH TASK TO THAT CALENDAR IT AUTOMATICALLY EXPORTS IT OUT AND IMPORTS IT INTO GOALGETTR, ie, TWO WAY IMPORT/EXPORT FOR BOTH))
		.col-sm-6
			.panel.panel-primary
				.panel-heading
					%h3.panel-title.text-center
						Task locations
				.panel-body
					/ %div
					/ 	#map{style: "width: 100%; height: 200px;"}
					/ 		= render "map"



	/ #i1_u0.quadrant
	/ 	%ol.sortable
	/ 		- @i1u0_tasks.each do |t|
	/ 			%li.task
	/ 				= t.task_name
	/ #i1_u1.quadrant
	/ 	%ol.sortable
	/ 		- @i1u1_tasks.each do |t|
	/ 			= render 'task', t: t
	/ #i0_u0.quadrant
	/ 	%ol.sortable
	/ 		- @i0u0_tasks.each do |t|
	/ 			= render 'task', t: t
	/ #i0_u1.quadrant
	/ 	.arrowhead
	/ 		%ol.sortable
	/ 			- @i0u1_tasks.each do |t|
	/ 				= render 'task', t: t


.container
	.row
		.col-lg-offset-5.col-lg-2
			%h3 Today
	.row
		.col-xs-6#i1_u1_color
			%table.table
				.tbody#flowing
					%ul.list-unstyled
						- if @i1u1_tasks
							- @i1u1_tasks.each do |t|
								= render 'task_columns', t: t
		.col-xs-6#i1_u0_color
			%table.table
				.tbody#flowing	
					%ul.list-unstyled
						- if @i1u0_tasks
							- @i1u0_tasks.each do |t|
								= render 'task_columns', t: t
	.row
		.col-xs-6#i0_u1_color
			%table.table
				.tbody#flowing
					%ul.list-unstyled
						- if @i0u1_tasks
							- @i0u1_tasks.each do |t|
								= render 'task_columns', t: t
		.col-xs-6#i0_u0_color
			%table.table
				.tbody#flowing	
					%ul.list-unstyled
						- if @i0u0_tasks
							- @i0u0_tasks.each do |t|
								= render 'task_columns', t: t
